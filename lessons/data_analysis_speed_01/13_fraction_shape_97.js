(()=>{const {q,c,add,R,pk,n,o,ds,sm,rs}=DA;add(c('fraction_shape_97','分数骨架｜补 0 与 9/7 分母',[
q('q33','补 0 转化',['技巧'],()=>{const den=pk([18,24,28,30,40,42,45,48,54,56,63,72]),num=n(1,Math.max(1,Math.floor(den/12))),ans=ds(num/den,6),padded=num*10,frac=sm(padded,den),big=ds(padded/den,4);const d=[num+1,num+2,Math.max(1,num-1),Math.max(1,num+3)].map(v=>ds(v/den,6));d.push(ds(num/(den+n(3,9)),6));return R(`${num}/${den} 通过补 0 转化后，约等于多少？`,ans,o(ans,d),`给 ${num}/${den} 的分子补 0，看成 ${padded}/${den}。`,`${padded}/${den} = ${frac} ≈ ${big}，所以 ${num}/${den} ≈ ${ans}。`)}),
q('q34','×9 分母骨架',['基础'],()=>{const k=pk([2,7,11,12]),den=k*9,base=String(Math.round(1000/k)).padStart(3,'0'),ans=String(Math.round(10000/den)).padStart(3,'0');const near=[ans*1+n(12,35),Math.max(1,ans-n(12,30)),Math.round(10000/(den+9)),Math.round(10000/(den-9||den))].map(x=>String(Math.abs(x)).padStart(3,'0'));return R(`1/${den} 的最终骨架大约接近哪一个？`,ans,o(ans,near,6),`${den} = ${k} × 9。这题不是背小数，而是先转成常用分数，再借助“除以 9”的骨架。`,`先看 1/${k} 的骨架 ${base}，再除以 9，${base} ÷ 9 ≈ ${rs(Number(base)/9,1)}，补位后是 ${ds(1/den,4)}，最终骨架 ${ans}。`)}),
q('q35','×7 分母骨架',['基础'],()=>{const k=pk([2,3,8,9,10,11,12]),den=k*7,base=String(Math.round(1000/k)).padStart(3,'0'),ans=String(Math.round(10000/den)).padStart(3,'0');const near=[ans*1+n(10,32),Math.max(1,ans-n(10,28)),Math.round(10000/(den+7)),Math.round(10000/(Math.max(7,den-7)))].map(x=>String(Math.abs(x)).padStart(3,'0'));return R(`1/${den} 的最终骨架大约接近哪一个？`,ans,o(ans,near,6),`${den} = ${k} × 7。先拆出熟悉的基础分数，再借助“除以 7”的骨架推导。`,`先看 1/${k} 的骨架 ${base}，再除以 7，${base} ÷ 7 ≈ ${rs(Number(base)/7,1)}，补位后是 ${ds(1/den,4)}，最终骨架 ${ans}。`)})
],'sequence'));})();
