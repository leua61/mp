(()=>{const {q,c,add,R,pk,n,o,fc}=DA;add(c('line_method_error_cases','划线法｜抵消叠加与例题',[
q('q53','抵消与叠加',['运用'],()=>{const p=n(2,7),qv=n(p+1,9),m=pk(['sameDown','sameUp','diff1','diff2']);let desc,ans,hint,sol,total;if(m==='sameDown'){total=qv-p;desc=`分子下降 ${p}%，分母下降 ${qv}%`;ans=`同向，误差抵消，剩余约 ${total}%`;hint='分子分母都下降，是同向变化。';sol=`同向变化误差抵消：${qv}% - ${p}% = ${total}%，剩余误差按分母方向修正。`}else if(m==='sameUp'){total=qv-p;desc=`分子上升 ${p}%，分母上升 ${qv}%`;ans=`同向，误差抵消，剩余约 ${total}%`;hint='分子分母都上升，是同向变化。';sol=`同向变化误差抵消：${qv}% - ${p}% = ${total}%，剩余误差按分母方向修正。`}else if(m==='diff1'){total=p+qv;desc=`分子上升 ${p}%，分母下降 ${qv}%`;ans=`异向，误差叠加，约 ${total}%`;hint='分子上升会使结果变大，分母下降也会使结果变大。';sol=`两个方向对结果的影响同为变大，所以叠加：${p}% + ${qv}% = ${total}%。`}else{total=p+qv;desc=`分子下降 ${p}%，分母上升 ${qv}%`;ans=`异向，误差叠加，约 ${total}%`;hint='分子下降会使结果变小，分母上升也会使结果变小。';sol=`两个方向对结果的影响同为变小，所以叠加：${p}% + ${qv}% = ${total}%。`}return R(`A÷B 中，${desc}，应如何处理？`,ans,o(ans,[`同向，误差抵消，剩余约 ${p+qv}%`,`异向，误差叠加，约 ${Math.abs(qv-p)}%`,'不需要修正','只看分子','只看分母'],5),hint,sol)}),
q('q54','例题',['运用'],()=>{const niceB=pk([600,800,900,1200,1500,2000,3000,4000,5000,6000,7000,8000]),p=n(2,8),sign=pk([-1,1]),b=Math.round(niceB*(1+sign*p/100)),a=n(5000,9800),est=a/niceB,real=a/b,ans=`约 ${fc(real)}`,pct=Math.round(Math.abs(b-niceB)/b*100),denomDir=niceB<b?'下降':'上升',fix=niceB<b?'下降':'上升';const wrong=[real*0.9,real*1.08,est,real*1.15].map(v=>`约 ${fc(v)}`);return R(`${a} ÷ ${b}，把 ${b} 看成 ${niceB}，修正后结果大约是？`,ans,o(ans,wrong),`${b} 变 ${niceB}，分母${denomDir}约 ${pct}%。`,`先算 ${a} ÷ ${niceB} ≈ ${fc(est)}。分母改${niceB<b?'小':'大'}导致估算偏${niceB<b?'大':'小'}，真实值${fix}约 ${pct}%，约 ${fc(real)}。`)})
],'sequence'));})();
